{
  "name": "apps",
  "private": true,
  "version": "0.0.0",
  "homepage": "https://apps.felipebabel.com",
  "scripts": {
    "dev": "npx serve .",
    "deploy": "git add -A && git commit -m 'deploy' --allow-empty && git push origin main && gh-pages -d ."
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
